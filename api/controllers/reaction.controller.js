var Reaction = require('../../models/reaction.model');

module.exports.count = (req, res) => {
  // const countLike = Reaction.count({ postId: somePostId, type: 'like' });

  // res.json(countLike);
}